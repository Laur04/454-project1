program halfAdder
  pre a = 0 || a = 1
  pre b = 0 || b = 1
  post s = (a + b) % 2
  post c = a * b
is
  boolSum := a + b;
  carry := 0;
  if boolSum > 1 then
    boolSum := 0;
    carry := 1;
  end
end